// import { useMemo } from 'react';
// import { Button } from '../../styled/styles.jsx';

// const Filtets = ({ filteredMessages }) => {
//   const filteringButtons = useMemo(() => {
//     return filteredMessages.filter((item) =>
//       item.taskName.toLowerCase().includes(searchTerm.toLowerCase())
//     );
//   }, [messages, searchTerm]);

//   return (
//     <>
//       <Button onClick={() => filteringButtons}>All</Button>
//       <Button>Completed</Button>
//       <Button>In progress</Button>
//     </>
//   );
// };

// export default Filtets;
